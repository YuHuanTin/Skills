---
name: reverse-enginer-skill
description: 逆向习惯
---

使用 python 一定要使用 venv 环境的 python，如果没有则创建

在执行过程中如果有需要抉择的地方，尽快使用 ask 工具提出(如果没有则都尝试一下)，ask 工具需要传递 array 类型的 questions

# 文档编写

编写 markdown 时语言精炼、清楚、低 AI 率，不使用生僻词；中文为主，技术术语/缩写/函数名/符号/命令保持原文(缩写术语首次出现解释一次)

可使用表格对结构化数据做展示，不使用无序列表

## 层级与命名
- 在第一个章节之前写全局对照表，用三个表格记录原类、方法、字段名和重命名后的名字，后续文档编写均使用替换之后的名字，如果类命名为新名字，方法、字段依旧使用原类名，尽可能不写 other 注释
  ```
  |class name|new name|other|
  |---|---|---|
  |aaa|A||

  |func name|new name|other|
  |---|---|---|
  |aaa.f|F||

  |field name|new name|other|
  |---|---|---|
  |aaa.i0|iType||
  ```
- `#` 文档主题；`##` 大章节，按模块/阶段/步骤划分；`###` 子章节
- 长分析文档用编号章节(`## 1. xxx` / `### 1.1 xxx`)，便于交叉引用(见 xxx 章节)
- 子章节直接用函数名、或步骤名作标题(`### IO_8001200C`、`### unpackData`、`### 刷入 img`)

## 小节布局
1. 一句话简述功能/目的/步骤("初始化"、"移动，主要逻辑"、"生成密钥")
2. 贴真实代码/命令/配置，反编译输出原样保留(含 v4/sub_xxx 等符号)
3. 必要时补一句分析或主次区分(如"重点在驱动交互，其余均为负责输入的逻辑")
4. 涉及调用链时画调用栈图

## 代码与命令
- 算法/流程用伪代码块或真实代码，函数名英文、描述中文
- 关键地址、偏移、patch 命令直接给出(VA 0x1400026BD，RVA 0x26BD；`eq ShadowGateSys+0x26bd c483489090909090`)
- 命令连同输出一起贴，保留提示符(`>` / `#` / `$`)

## 绘图

调用栈与流程图、进程/模块间数据流等需要图展示的，编写 mermaid 代码进行渲染

## 推测与证据
- 推测性结论明确标注("猜测"、"可能"、"存疑")，结合实际代码/字符串/符号论证
- 分析技巧记录在文档中(如难分析的 call 先 nop 掉看整体流程、用花指令魔数定位泄漏点)
- 重要结论用 **加粗** 或 blockquote `>` 强调；废弃思路用 `~~删除线~~`

## 维护
- 分析完成后总是编写或修改关键总结 markdown
- 若文档已存在，核对与最新结论是否一致，不符则修正
- 长文档章节较多时开头写一段导言，说明章节顺序与阅读路径

# 通用规则

在确定的情况下函数内容的情况下可以对函数重新命名，在分析大函数时，可对其中的小函数进行重命名

在确定的情况下可简短的重命名函数内的变量、全局变量名

## ida

### 连接选择

1. 检查当前会话是否存在原生 IDA MCP tools，并调用 `idb_list` 确认连接可用，可用时直接使用，不额外启动服务
2. 原生 IDA MCP 不存在或无法连接时，后台启动 `idalib-mcp --host 127.0.0.1 --port 8745 --max-workers 4`，等待就绪后继续，任务结束时不关闭
3. 使用 venv Python 运行 [scripts/using_mcp.py](scripts/using_mcp.py) 调用 HTTP tool

放宽 ida 操作的超时时间

在确定的情况下可新建对类、常见结构、可能有助于分析的结构的结构体类型，对变量进行类型设置后帮助分析

ida 批量重命名函数后创建一个函数分组，将原来的函数移动到分组内，注意是剪贴而不是复制一份

## jeb pro

可以使用 jeb_mcp.py 来连接到 jeb mcp

## jadx

> 配置
如果没有配置 mcp，则添加后重启
```
"jadx-mcp": {
  "type": "http",
  "url": "http://127.0.0.1:8651/mcp"
}
```
>

连接 jadx mcp 用 jadx_mcp_server.py。每次写操作后必须等缓存刷新再继续；批量操作须写脚本、插入延迟、用后即删。

### 传参写法

一律用当前显示名，不用 dex 旧名，jadx 源码顶部出现 `/* JADX INFO: renamed from: 旧全限定名 */` 即已改名。此后 `get_*`/`rename_*` 的 `class_name`/`method_name` 必须用新名(例 `g1`→`MMAutoAuth`、`e`→`sendImp`)。`get_method_by_name` 的 `decl` 字段是旧 dex 签名，禁止用于寻址

| 工具 | 必传 | 硬规则 |
|------|------|--------|
| `rename_class` | `class_name`(全限定 `com.a.B.foo`)、`new_name`(不含包) | |
| `rename_method` | `method_name`(全限定)、`new_name` | 有重载或大小写同名(`k`/`K`)必传 `method_signature`；签名只传 dex 描述符 `(Lcom/a/z0;)I` 格式 |
| `rename_field` | `class_name`(全限定)、`field_name`、`new_name` | |
| `rename_variable` | `class_name`(全限定)、`method_name`(裸名 `onCreate`)、`variable_name`、`new_name` | 局部变量和形参都能改；每次仅改一个，改完立即 `get_method_by_name` 取剩余变量当前显示名 |

- `@Override` 方法：rename 主绑定接口/父类，即源码 `@Override // A, B` 里列的第一个类型。改主绑定即传播到所有实现类；改前先 `get_method_by_name` 看 `@Override //` 首个类型。字段同理改声明方

### rename_variable 的 variable_name 取法
显示名 = 内部基名 + 尾部寄存器/序号

`variable_name` 传法：
1. 显示名不以纯数字结尾(`z0Var`/`o0Var`/`iK`)→ 直接传完整显示名，通常无需 `reg`
2. 显示名以数字结尾(`i16`/`th6`)→ 去掉尾部数字传基名(`i16`→`i`，`th6`→`th`)。改后新名仍会被加回 reg 号显示(`th`→显示`ex6`)
3. 基名有歧义(多个同基名变量，如多个 `i` 开头 int)→ 基名+`reg`(smali 寄存器号)消歧。寄存器号算法：`.registers R`、共 `P` 个参数(窄类型各占 1 寄存器)，则第 `N` 个参数(N 从 1 计，static 方法首参即 N=1)的寄存器 = `v(R - P + N - 1)`。例：`sendImp` 第 4 参数、R=13、P=4 → `v12` → `variable_name="i"` + `reg=12`；`d` 第 3、4 参数、R=11、P=5 → `v8`/`v9` → `reg=8`/`reg=9`。显示名尾数字不是 reg，且改掉一个后剩余同基名变量的显示名尾数字会变，使用 reg 稳定定位
4. 失败时依次排查：显示名是否被重编号(重拉源码)→ 是否该去尾数字取基名 → 是否需加 `reg` → `clear_cache` 后重试。禁止把完整显示名 `i16` 或 smali 名 `p3`/`v12` 直接当 `variable_name`

# 重构代码文件

遇到重构代码要求时，对指定代码文件进行可读化重写、可重命名变量名、字段名、函数名

对重命名后的函数在上方进行注释，内含有原来方法名、函数简单功能描述介绍

文件开头写入所有描述信息、总结信息，最后不要输出内容到对话输出中
