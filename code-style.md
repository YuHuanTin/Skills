---
name: code-style-skill
description: 代码风格（编码、布局、注释、测试）
---

# 代码风格

- 只用 utf-8 编码，不允许 BOM，中文不能出现乱码或 ??
- 精简代码时不要乱删注释；代码改了，注释跟着一起改
- 不要过度写测试，只在明说的情况下写有意义的测试，不写细枝末节的测试
- 同时检查有没有冗余的代码和字段，有就清理掉，尽可能复用代码
- 除非用户说明，否则不捏造，不瞎猜，错误路径或不符合用户要求时写抛错代码用于排除问题而不编写强行兜底代码
- 模块级初始化代码必须集中放置，禁止夹在函数或类定义之间；依赖函数或类的初始化统一放在所有依赖定义之后
- 全局对象必须在其依赖的枚举、函数或类定义之后初始化
- 导入只放在文件顶部，不在函数或类内部导入
- 代码布局为
```
模块说明
导入

不依赖函数或类的枚举、常量、映射、对象

内部函数
类

依赖函数或类的全局对象初始化
导出函数
主程序入口（模块则没有）
```

## Python

形参不换行，尽可能写在同一行；空行数量用 1，不用 2

返回值类型是 None 就不标注；不用 `__all__` 暴露函数

使用 `-B` 或设置 `PYTHONDONTWRITEBYTECODE=1` 运行，发现已有 __pycache__ 时删除

## 成员访问

- 禁止使用 `getattr(..., default)`、`hasattr()` 等防御式成员访问。所有正式成员必须直接访问；缺失成员应立即抛出异常，不编写强行兜底代码
