---
name: code-style-skill
description: 代码风格与写作风格
---

# 代码风格

只用 utf-8 编码，不允许 BOM，中文不能出现乱码或 ??

精简代码时不要删注释；代码改了，注释跟着一起改

不要过度写测试，只在明说的情况下写有意义的测试，不写细枝末节的测试

同时检查有没有冗余的代码和字段，有就清理掉

代码布局为

```
全局变量
内部函数
类
    类变量
    类行数
导出函数
```

## Python

形参不换行，尽可能写在同一行；空行数量用 1，不用 2

返回值类型是 None 就不标注；不用 `__all__` 暴露函数

执行完编译后删除 `__pycache__` 文件夹

# 通用写作指令

写文字和做界面是同一回事：每个成分都得有存在的理由，好文字每一句都删不掉。生成任何中文文本时，按下面的准则写。

每句话必须经得起两个检验：删掉它，意思是否有损失；读者追问「这具体指什么」，你能否用事实或逻辑答上来。过不了的句子不要写。以下七条都是这两个检验的展开。

## 一、不强调无需强调的事

正常读者不会误解的方向，不要特地澄清。「我会认真帮你完成（而不是敷衍你）」，没人觉得你会敷衍，这句话暴露的是心虚。想写「不是 X」之前，先问真有人会误解成 X 吗，没有就删掉整句。

## 二、修饰必须承载信息

删掉一个形容词或从句后句意不变，它就不该在那里。想说「早上起床吃早饭」，不要写成「起床进行必不可少的、用以缓解早间饥饿的早餐环节」。不要为文采保留通不过删除测试的修饰。

## 三、比喻和术语必须经得起追问

「自断手腕」「把 Shell 变成认知流编辑器」这类说法，要么把需要论证的判断包装成常识，逃掉解释的责任，要么掩盖内容的贫乏，追问一句就发现底下什么都没有。需要造新词撑场面，多半是自己还没想清楚。

帮不到理解、只作花哨替换的比喻同样是病，如「把裂缝补上」「旋钮」「免费午餐」「思考口头禅」，都该改成直陈：「消除分布不匹配」「连续参数」「主导训练信号」。唯一例外是原文献作者自己使用、有明确出处的比喻，那是作者的论证方式，可以保留。

写比喻前必须能回答两件事：能否用具体事实展开它；它是否确实比直白说法更帮助理解。答不上就直说。

## 四、多余的东西要删掉，不是修好

电梯外侧多出的开门键，正确做法是删掉，不是补一套「不在当前楼层时提示无法开门」的逻辑。写作同理：发现某段内容需要额外解释才站得住，先判断它该不该存在，不该存在的直接删，连补丁一起删。

## 五、不要制造假的对立

「这不是降价，而是商业模式的革命」，被否定的半句没人提出过，是作者凭空立起来再推倒，制造「纠正了你的误解」的深刻感。除非 X 是读者真实存在的误解，否则不用「不是 X 而是 Y」的句式，直接说 Y。

## 六、不要用夸大来代替论证

「彻底重塑行业」「定义下一个时代」，用一个听起来很重的判断逃掉本该给出的论证。一件事重要在哪，需要具体讲清楚，喊得越响，底下越空。

更廉价的形态是用情绪词反复升温：「全网炸了」「真正炸裂的部分」，读者被反复告知这很厉害，却始终不知道厉害在哪。「炸」「震撼」「颠覆」这类词只提供音量不提供信息。每个重的断言都落到具体事实和后果上，落不下来就把调门降到你能支撑的程度。

## 七、用词和标点保持通用

用普通读者一眼就懂的常用词，不用生僻、书面腔过重、文言半文言的表达。标点以句号、逗号、顿号、问号、引号、冒号为主，不用破折号、间隔号、着重号。不写一两个字加冒号的小标签式行文，把意思放进完整的句子里说。
